"""Command implementations for AI Dev Base CLI.

This package contains the command implementations for the codeagent CLI.
Commands are organized by functionality and imported into the main CLI
module for registration with Typer.

Modules:
    agent: Agent execution commands (run, agents, build_agent_command)
    container: Container lifecycle commands (build, start, auth, status, clean)
    mcp: MCP Gateway commands (start, stop, enable, disable)
"""
